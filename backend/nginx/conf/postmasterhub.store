server {
    listen 80;
    listen [::]:80;

    server_name postmasterhub.store www.postmasterhub.store localhost;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://postmasterhub.store$request_uri;
    }
}
